### 문자열 내 마음대로 정렬하기 - Lv 1

### ⏰ 풀이 시간(min) - 40min

### 🤔 스스로 고민한 시간(min) - 20min

### 🧰 접근 방법

- strings 배열을 sort()의 비교 함수를 이용해 정렬한다.
- 두 문자열 a, b를 비교할 때 먼저 a[n]과 b[n]을 비교한다.
- 만약 n번째 문자가 같다면 문자열 전체를 사전순(localeCompare)으로 비교한다.
- 이렇게 만든 정렬 기준으로 배열을 정렬한 뒤 결과를 반환한다.
